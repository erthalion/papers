def get_data(self):
    data = {}
    if self.obj_id:
        # do something with data[]
    else:
        if self.item_id:
            # do something with data[]
        else:
            # do something with data[]
        data["questions"] = process_questions()
        data["answers"] = process_choices()
        # do something
    return data
